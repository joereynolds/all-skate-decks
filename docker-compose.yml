version: '3'

services:
  php:
    build:
      context: ./infrastructure/docker/php-apache/
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
  node:
    image: node:18
  database:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: all-skate-decks
